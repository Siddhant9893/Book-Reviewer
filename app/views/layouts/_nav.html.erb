<nav class="navbar bg-dark border-bottom border-body fixed-top" data-bs-theme="dark">
  <div class="container-fluid">
  <a class="navbar-brand" href="#"><b>Book<b></a>
  <% if user_signed_in? %>
    <%= button_to "Log Out", destroy_user_session_path, method: :delete, data: { turbo_method: :delete }, class:"btn btn-outline-danger" %>
  <% else %>
  <div class="d-grid gap-2 d-md-flex justify-content-md-end" class="d-grid gap-2 d-md-block">
    <h3><%= link_to "Login", new_user_session_path, class: "btn btn-outline-primary "  %></h3>
    <h3><%= link_to "Sign Up", new_user_registration_path, class: "btn btn-outline-primary " %></h3>
  </div>
<% end %>
  </div>
</nav>

