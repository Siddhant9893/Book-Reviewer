<h1>Admin Dashboard</h1>
<h2 class="mb-4"><br>All Books</h2>

<table class="table table-striped">
  <thead>
    <tr>
      <th>#</th>
      <th>Title</th>
      <th>Author</th>
      <th>Email</th>
      <th>View</th>
    </tr>
  </thead>
  <tbody>
    <% @books.each_with_index do |book, index| %>
      <tr>
        <th scope="row"><%= index + 1 %></th>
        <td><%= book.title %></td>
        <td><%= book.author %></td>
        <td><%= book.user.email %></td>
        <td> <%= link_to "View", admin_path(book), class: "btn btn-outline-primary" %></td>
      </tr>
    <% end %>
  </tbody>
</table>
